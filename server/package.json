{
  "name": "age-verification-server",
  "version": "0.1.0",
  "private": true,
  "description": "Age provider backend — holds private keys, publishes public keys, signs DoB claims.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
